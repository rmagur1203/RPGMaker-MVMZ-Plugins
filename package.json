{
  "name": "root",
  "private": true,
  "npmClient": "yarn",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "inject": "lerna run build && lerna run inject --concurrency=1"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.1",
    "typescript": "^5.2.2"
  }
}